import {SafeAreaView} from "react-native-safe-area-context";
import {Text} from "react-native";

export default function TeamMembersScreen() {
    return (
        <SafeAreaView>
            <Text>TeamMembersScreen</Text>
        </SafeAreaView>
    )
}